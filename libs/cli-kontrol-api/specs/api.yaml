openapi: 3.0.0
info:
  title: Simple API
  version: 1.0.0
paths:
  /greet:
    get:
      summary: Greet
      operationId: greet
      responses:
        "200":
          description: Successful Response
          content:
            application/json:
              schema:
                type: string
